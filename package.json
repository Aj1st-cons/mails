{
  "name": "vercel-email",
  "version": "1.0.0",
  "description": "A serverless function to send emails using Resend API on Vercel",
  "main": "api/send-email.js",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel"
  },
  "dependencies": {
    "node-fetch": "^3.3.1"
  },
  "engines": {
    "node": ">=16"
  }
}
